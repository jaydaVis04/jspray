[paths]
database = "/var/tmp/samsung-fw-sync-live/database/firmware.db"
downloads = "/var/tmp/samsung-fw-sync-live/downloads"
extracted = "/var/tmp/samsung-fw-sync-live/extracted"
cache = "/var/tmp/samsung-fw-sync-live/cache"
state = "/var/tmp/samsung-fw-sync-live/state"

[discovery]
sources = ["samfrew", "sammobile"]
lookback_days = 21

[metadata]
path = "/var/tmp/samsung-fw-sync-live/metadata.json"
append_completed = true

[download]
automatic = false
automatic_extract = false
concurrency = 1
connections_per_file = 1
minimum_free_bytes = 16106127360
samloader_executable = "/tools/samloader"
command_timeout_seconds = 300
